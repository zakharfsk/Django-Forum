# Django Forum

Сучасна платформа для обговорень з системою модерації та рольовим контролем доступу.

![Django](https://img.shields.io/badge/Django-6.0-green.svg)
![Python](https://img.shields.io/badge/Python-3.13+-blue.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Latest-blue.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## 📋 Зміст

- [Основні можливості](#-основні-можливості)
- [Технологічний стек](#-технологічний-стек)
- [Швидкий старт](#-швидкий-старт)
- [Конфігурація](#-конфігурація)
- [Деплоймент](#-деплоймент)
- [Структура проекту](#-структура-проекту)
- [Документація](#-документація)
- [Ліцензія](#-ліцензія)

## ✨ Основні можливості

### Для користувачів:
- 📝 Створення топіків та публікація відповідей з Rich Text редактором (CKEditor 5)
- 📁 Ієрархічна структура категорій (необмежена вкладеність)
- 👤 Персональні профілі з аватарами та біографією
- 🔍 Повнотекстовий пошук по топіках та постах
- 📊 Статистика активності користувачів

### Для модераторів:
- ✅ Система модерації контенту (схвалення/відхилення топіків)
- 📌 Закріплення важливих топіків
- 🔒 Закриття топіків
- 🚫 Бан/розбан користувачів
- 📜 Повна історія дій модерації

### Адміністрування:
- 👑 Рольова система контролю доступу (RBAC)
- 🎭 6 попередньо визначених ролей: OWNER, ADMINISTRATOR, MODERATOR, VIP, MEMBER, BANNED
- 🔐 8 типів прав доступу з гнучким налаштуванням
- 📈 Детальна панель адміністратора Django

## 🛠 Технологічний стек

### Backend
- **Django 6.0** - Web framework
- **PostgreSQL** - База даних
- **Python 3.13+** - Мова програмування

### Frontend
- **Bootstrap 5.3** - CSS framework
- **CKEditor 5** - Rich text editor
- **Bootstrap Icons** - Іконки

### Інструменти
- **Gunicorn** - WSGI сервер
- **Pillow** - Обробка зображень
- **dj-database-url** - Database URL parsing
- **environs** - Управління змінними оточення

## 🚀 Швидкий старт

### Вимоги
- Python 3.13 або новіший
- PostgreSQL 12+ (або SQLite для розробки)
- pip або uv package manager

### Інсталяція

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/yourusername/DjangoForum.git
cd DjangoForum
```

2. **Створіть віртуальне середовище:**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **Встановіть залежності:**
```bash
pip install -r requirements.txt

# Або з uv (швидше)
uv pip install -r requirements.txt
```

4. **Налаштуйте змінні оточення:**
```bash
# Створіть .env файл
cp .env.example .env

# Відредагуйте .env та встановіть свої значення
```

5. **Застосуйте міграції:**
```bash
python manage.py migrate
```

6. **Ініціалізуйте ролі:**
```bash
python manage.py init_roles
```

7. **Створіть суперюзера:**
```bash
python manage.py createsuperuser
```

8. **Запустіть сервер розробки:**
```bash
python manage.py runserver
```

9. **Відкрийте в браузері:**
```
http://127.0.0.1:8000
```

Адмін-панель доступна за адресою: `http://127.0.0.1:8000/admin`

## ⚙️ Конфігурація

### Змінні оточення (.env)

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/djangoforum
# Або для розробки:
# DATABASE_URL=sqlite:///db.sqlite3

# Superuser (для автоматичного створення при деплойменті)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=securepassword123
```

### База даних

**SQLite (для розробки):**
```env
DATABASE_URL=sqlite:///db.sqlite3
```

**PostgreSQL (рекомендовано для продакшну):**
```env
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
```

### Статичні та медіа файли

```bash
# Збірка статичних файлів
python manage.py collectstatic --noinput
```

Статичні файли зберігаються в `staticfiles/`, медіа файли в `media/`.

## 🌐 Деплоймент

### OnRender

1. **Створіть новий Web Service на OnRender:**
   - Підключіть GitHub репозиторій
   - Environment: `Python 3`
   - Build Command: `./deploy.sh`
   - Start Command: `gunicorn config.wsgi:application`

2. **Додайте Environment Variables:**
   ```
   SECRET_KEY=your-production-secret-key
   DEBUG=False
   ALLOWED_HOSTS=.onrender.com,yourdomain.com
   DATABASE_URL=<postgres-connection-string>
   DJANGO_SUPERUSER_USERNAME=admin
   DJANGO_SUPERUSER_EMAIL=admin@example.com
   DJANGO_SUPERUSER_PASSWORD=secure-password
   PYTHON_VERSION=3.13
   ```

3. **Створіть PostgreSQL базу даних:**
   - OnRender → New → PostgreSQL
   - Скопіюйте Internal Database URL в DATABASE_URL

4. **Deploy:**
   - OnRender автоматично виконає деплоймент при push в GitHub

### Docker

```bash
# Build
docker build -t djangoforum .

# Run
docker run -p 8000:8000 --env-file .env djangoforum
```

Або з docker-compose:
```bash
docker-compose up -d
```

### Ручний деплоймент

```bash
# Виконайте скрипт деплойменту
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматично:
- Встановить залежності
- Зробить міграції
- Збере статичні файли
- Створить суперюзера
- Ініціалізує ролі

## 📁 Структура проекту

```
DjangoForum/
├── apps/
│   ├── forum/              # Функціонал форуму (категорії, топіки, пости)
│   └── users/              # Користувачі, ролі, профілі
│
├── config/                 # Django конфігурація
│   ├── settings.py         # Налаштування проекту
│   ├── urls.py             # Головні URL маршрути
│   ├── wsgi.py             # WSGI конфігурація
│   └── asgi.py             # ASGI конфігурація
│
├── templates/              # HTML шаблони
├── static/                 # Статичні файли (CSS, JS)
├── staticfiles/            # Зібрані статичні файли
├── media/                  # Завантажені файли
│
├── deploy.sh               # Скрипт деплойменту
├── manage.py               # Django CLI
├── requirements.txt        # Python залежності
└── .env.example            # Приклад змінних оточення
```

## 📖 Документація

### Основні команди

```bash
# Створення міграцій
python manage.py makemigrations

# Застосування міграцій
python manage.py migrate

# Ініціалізація ролей
python manage.py init_roles

# Створення суперюзера
python manage.py createsuperuser

# Запуск dev сервера
python manage.py runserver

# Збірка статичних файлів
python manage.py collectstatic

# Відкрити Django shell
python manage.py shell

# Перевірка проекту
python manage.py check

# Перевірка безпеки для продакшну
python manage.py check --deploy
```

### Ролі та права доступу

| Роль | Рівень | Опис |
|------|--------|------|
| **OWNER** | 100 | Повний контроль над системою |
| **ADMINISTRATOR** | 50 | Адміністративний доступ |
| **MODERATOR** | 30 | Модерація контенту |
| **VIP** | 20 | Звичайний користувач з особливим статусом |
| **MEMBER** | 10 | Звичайний користувач (за замовчуванням) |
| **BANNED** | 0 | Заблокований користувач |

### Права доступу (8 типів):
- `can_edit_any_post` - Редагування будь-яких постів
- `can_delete_any_post` - Видалення будь-яких постів
- `can_close_topics` - Закриття топіків
- `can_pin_topics` - Закріплення топіків
- `can_manage_users` - Управління користувачами
- `can_manage_categories` - Управління категоріями
- `can_ban_users` - Бан/розбан користувачів
- `can_moderate_topics` - Модерація топіків

### Процес модерації

1. Користувач створює топік → статус `PENDING`
2. Топік видимий тільки автору та модераторам
3. Модератор переглядає черзу модерації
4. Модератор може:
   - **Схвалити** (APPROVED) → топік стає видимим усім
   - **Відхилити** (REJECTED) з коментарем → видимий тільки автору
5. Всі дії зберігаються в історії модерації

## 🔐 Безпека

### Рекомендації для продакшну:

```python
# settings.py
DEBUG = False
SECRET_KEY = 'strong-random-secret-key'
ALLOWED_HOSTS = ['.onrender.com', 'yourdomain.com']

# HTTPS
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
```

### Перевірка безпеки:
```bash
python manage.py check --deploy
```

## 🤝 Контрибуція

1. Fork проекту
2. Створіть feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit зміни (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Відкрийте Pull Request

## 📝 Детальна технічна документація

Повна технічна документація для розробників та AI доступна в файлі [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md).

Цей файл містить:
- Повний опис архітектури
- Детальну інформацію про моделі даних
- Схеми зв'язків між таблицями
- Опис всіх views, forms, templates
- Конфігурацію та налаштування
- Інструкції з деплойменту
- Рекомендації з оптимізації та безпеки

## 📧 Контакти

- **Email:** zakhar.fesik@gmail.com
- **GitHub:** [@zakharfsk](https://github.com/zakharfsk)

## 📄 Ліцензія

Цей проект ліцензовано під MIT License - див. файл [LICENSE](LICENSE) для деталей.

---
